# 17 — Perguntas de revisão HTML

1. Para que serve o `doctype`?
2. Por que `lang="pt-BR"` é importante?
3. Qual é a diferença entre conteúdo do `head` e do `body`?
4. Quando usar `section` e quando usar `article`?
5. Por que `div` não deve ser a primeira escolha para tudo?
6. O que torna um `title` ruim?
7. Qual é a função da `meta description`?
8. Por que `placeholder` não substitui `label`?
9. Quando usar `button` em vez de `a`?
10. Como escrever um bom `alt`?
11. Para que serve `scope` em tabelas?
12. O que significa manter headings em ordem lógica?
13. Quando usar `aria-*`?
14. Quais problemas surgem ao usar `table` para layout?
15. Como validar a qualidade estrutural de um HTML?

## Autoavaliação

- Consigo montar uma página HTML completa sem copiar?
- Consigo justificar a escolha de cada tag estrutural?
- Consigo encontrar problemas de acessibilidade em formulários?
- Consigo revisar SEO básico apenas olhando o HTML?
