# Perguntas de revisão CSS

1. Qual é a responsabilidade do CSS em relação ao HTML?
2. Por que CSS externo é preferível em projetos profissionais?
3. Qual é a diferença entre classe e ID?
4. Quando um seletor descendente pode se tornar frágil?
5. O que é cascata?
6. O que é especificidade?
7. Quais propriedades costumam ser herdadas?
8. Quando `!important` pode ser aceitável?
9. Qual é a diferença entre margin e padding?
10. Para que serve `box-sizing: border-box`?
11. Como evitar contraste ruim entre texto e fundo?
12. Por que `line-height` é importante?
13. Quando usar `rem` em vez de `px`?
14. O que `display: none` faz no fluxo da página?
15. Qual é a diferença entre `visibility: hidden` e `opacity: 0`?
16. Por que `:focus-visible` é importante?
17. Para que servem `::before` e `::after`?
18. O que são custom properties?
19. Como variáveis ajudam na manutenção?
20. Quais sinais indicam CSS difícil de manter?
21. O que revisar antes de commitar CSS?
